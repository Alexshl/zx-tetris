# NN. <Название>

**Статус**: TODO
**Зависит от**: <id или —>
**Блокирует**: <id или —>

## Цель

<коротко — одним предложением, что должно работать по завершении>

## Контекст

<почему эта задача нужна, на что опирается, какие архитектурные решения затрагивает>

## Acceptance criteria

- [ ] ...
- [ ] ...

## Test plan

<!-- Выбрать ОДИН режим ниже, остальные секции удалить. -->
<!-- Без этой секции tester вернёт FAIL test-plan-mismatch. -->

### Вариант A — skip (задача не имеет рантайм-поведения для проверки)

```
skip: <причина, например "documentation only" или "infrastructure only">
```

### Вариант B — smoke-only (достаточно прогона make smoke)

```
smoke-only: <ожидание, например "build succeeds, smoke.scr=6912, PC≠0x0000">
```

### Вариант C — scenarios (прогон конкретных integration-сценариев)

```
scenarios: [spawn-piece, line-clear, game-over]
```

## Вне MVP

- <фичи или идеи, которые не входят в скоуп этой задачи, но могут быть полезны позже>
